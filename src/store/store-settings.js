const state = {
  settings: {
    show12HourTimeFormat: false,
  }
}

const mutations = {
}

const actions = {
}

const getters = {
  settings: state => state.settings,
}


export default {
  namespaced: true,
  state,
  mutations,
  actions,
  getters
}
